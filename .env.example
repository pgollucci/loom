# AgentiCorp Configuration - Copy to .env for local development
# This file contains sensitive configuration and should never be committed to git

# Default password for UI login and key encryption (development only)
# In production, use a strong password and change it immediately
AGENTICORP_PASSWORD=agenticorp-default-password

# Database configuration
DATABASE_URL=sqlite:./agenticorp.db
